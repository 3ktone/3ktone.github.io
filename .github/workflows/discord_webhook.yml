name: Discord Webhook Notification

on:
  push:
    branches:
      - main

jobs:

  notify-discord:
    runs-on: ubuntu-latest
    
    steps:
    - name: Send Discord Notification
      env:
        DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_API_WEBHOOK }}  # Corriger la référence ici
      run: |
        curl -X POST -H "Content-Type: application/json" \
        -d '{"content": "Un nouveau commit a été poussé sur la branche main 🚀 !"}' \
        "$DISCORD_WEBHOOK_URL"
